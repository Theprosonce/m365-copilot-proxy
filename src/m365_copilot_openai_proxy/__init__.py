"""Microsoft 365 Copilot OpenAI proxy."""

# Single source of truth for the version. Consumed by: pyproject (hatchling dynamic version),
# the tray app (APP_VERSION), and the build scripts (read via `python -c`).
__version__ = "0.6.1"
